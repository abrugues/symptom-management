<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="org.coursera.capstone.PatientMainMenuActivity" >

    <TextView
        android:id="@+id/checkinTxtView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="false"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:textSize="36sp"
        android:text="@string/check_in" />
    
    <ImageView
        android:id="@+id/checkinImgView"
        android:layout_width="147sp"
        android:layout_height="147sp"
        android:layout_below="@id/checkinTxtView"
        android:layout_marginTop="16dp"
        android:layout_alignParentLeft="false"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/check_in"
        android:src="@drawable/checklist" />
	
    <TextView
        android:id="@+id/reminderTxtView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/checkinImgView"
        android:layout_marginTop="24dp"
        android:layout_centerHorizontal="true"
        android:textSize="36sp"
        android:text="@string/set_reminders" />
    
    <ImageView
        android:id="@+id/reminderImgView"
        android:layout_width="147sp"
        android:layout_height="147sp"
        android:layout_below="@id/reminderTxtView"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"
        android:contentDescription="@string/set_reminders"
        android:src="@drawable/alarm_clock" />
    
</RelativeLayout>
